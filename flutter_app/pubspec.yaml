name: congmiao_toolbox_flutter
description: Congmiao Toolbox — Flutter port of the Tauri/Svelte desktop toolbox.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  file_selector: ^1.0.3
  path: ^1.9.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
